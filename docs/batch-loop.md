# 複数チケットの連続ループ実行ガイド

複数のactiveチケットを、各チケット完了時の承認ゲートで止めずに連続でループさせるための運用手順です。

## 前提：承認ゲートの仕組み

通常、チケットが reviewer承認で `done` に到達すると、orchestrator は停止して人間に報告し、改善ループの判断（実装ループ継続 / 設計見直し / 調査やり直し）を待ちます（`agents/orchestrator.md` の改善ループ判断テーブル、および Never ルール `Skip reporting to human after reviewer approval` / `Start improvement loop without human instruction`）。

「次のチケットに進む」こと自体が改善ループ判断テーブルの **「実装ループ継続」= 人間の判断** に当たるため、既定では1件ごとに承認ゲートが挟まります。

## 方法：ループ開始時にバッチ承認を与える

「実装ループ継続」の判断を**あらかじめ人間が下しておく**ことで、orchestrator は停止待ちをせず次チケットへ進めます。ポリシーの変更は不要で、既存ルールの枠内で成立します。

### 送る命令（テンプレート）

```
/start-loop {先頭チケットID}

【バッチ連続実行の事前承認】
{ID-1} と {ID-2} を、この順で連続してループさせてください。
- {ID-1} が reviewer承認で done に到達したら、成果物の要約だけ報告し、
  改善ループ判断は「実装ループ継続」として扱い、停止せず {ID-2} に進んでよい。
- {ID-2} が done に到達したら、そこで停止して報告し、私の判断を待つこと。
- 上記は {ID-1}→{ID-2} の遷移1回のみに適用する事前承認です。
  差し戻し（tester/reviewer）やリトライ上限超過・blocked が発生した場合は、
  この承認に関わらず直ちに停止して報告してください。
```

3件以上を連続させたい場合は、承認対象IDと「done到達時の扱い」を同様に列挙する。範囲を広げるほど人間の確認機会が減る点はトレードオフ。

### 各行の意図

| 文面 | 役割 |
|---|---|
| 「改善ループ判断は『実装ループ継続』として扱い」 | 改善ループ判断テーブルの人間判断を事前に充足し、停止待ちを解消する |
| 「成果物の要約だけ報告し」 | `Skip reporting to human after reviewer approval` の禁止を守る。報告は省かず、*待たない*だけにする |
| 「最終チケットで停止して待つ」 | 事前承認の範囲を限定し、無制限の自走を防ぐ |
| 「差し戻し・blocked時は直ちに停止」 | 異常系では安全装置を効かせる。事前承認は正常完了の前進遷移にのみ適用 |

## 制約・注意点

- **逐次実行であり並行ではない** — `One ticket, one active sub-agent at a time`（`agents/orchestrator.md`）により、1件を最後まで流してから次へ進む。同時並行はしない。
- **事前承認は明示した遷移のみに有効** — 範囲外のチケットには通常どおり承認ゲートが入る。
- **in_progress上限3件** — 連続対象が3件以内なら抵触しない（逐次なので同時は常に1件）。4件以上を一気に着手しようとすると上限チェックで停止する。
- **差し戻し・リトライ上限超過・blocked** が発生した場合は、バッチ承認に関わらず停止して報告される。
